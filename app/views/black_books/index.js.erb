$('#activities').append("<%= j render_activities @activities %>")
<% if @activities.next_page %>
	$('.activities').find('.pagination').replaceWith('<%= j will_paginate @activities %>');
<% else %>
	$('.activities').find('.see-more').remove();
<% end %>